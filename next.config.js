/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.sanity.io',
      'https://iconscout.com/lotties/circle?price=free',
    ],
  },
};

module.exports = nextConfig;
