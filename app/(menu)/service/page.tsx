import MenuHeader from '@/app/components/MenuHeader';
import React from 'react';

const page = () => {
  const title = 'service';
  return (
    <div>
      <MenuHeader title={title} />
    </div>
  );
};

export default page;
